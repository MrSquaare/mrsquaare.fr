"use client";

import {
  Icon as IconifyIcon,
  IconProps as IconifyIconProps,
  InlineIcon as IconifyInlineIcon,
} from "@iconify/react";

type IconProps = IconifyIconProps;

const Icon = IconifyIcon;

const InlineIcon = IconifyInlineIcon;

export { Icon, InlineIcon };
export type { IconProps };
