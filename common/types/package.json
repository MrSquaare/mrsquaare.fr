{
  "private": true,
  "name": "@common/types",
  "version": "1.0.0",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "eslint src/",
    "format": "eslint --fix src/",
    "clean": "rimraf dist/",
    "fclean": "npm run clean && rimraf node_modules/"
  },
  "devDependencies": {
    "typescript": "^5.1.6"
  }
}
