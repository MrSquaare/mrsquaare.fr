{
  "private": true,
  "name": "@common/types",
  "version": "1.0.0",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "eslint src/",
    "format": "eslint --fix src/",
    "clean": "rm -rf dist/",
    "fclean": "npm run clean && rm -rf node_modules/"
  },
  "devDependencies": {
    "typescript": "^4.7.4"
  }
}
